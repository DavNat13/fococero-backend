// ==========================================
// 📋 Estructura de rutas de ejemplo
// ==========================================
// Descomenta las lineas para activar rutas CRUD

// import { Router } from 'express';
// import { resourceController } from '../controllers/resource.controller';
// import { authenticate, authorize } from '../middlewares/auth.middleware';
// import { validate } from '../middlewares/validate.middleware';

// const router = Router();

// router.get('/resources', resourceController.getAll.bind(resourceController));
// router.get('/resources/:id', resourceController.getById.bind(resourceController));
// router.post('/resources', authenticate, validate(bodySchema), resourceController.create.bind(resourceController));
// router.put('/resources/:id', authenticate, resourceController.update.bind(resourceController));
// router.delete('/resources/:id', authenticate, resourceController.delete.bind(resourceController));

// export default router;

export {};